const assert = require('assert');
const dbClient = require('../dbClient');

__mocha().describe('DBClient Tests', () => {
   it('Specific DBClient Test', async () => {
        // Implement tests here
   });
});